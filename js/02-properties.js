/*
 * Властивості елемента (hero)
 * - Зображення
 * - Текст та textContent
 * https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480
 */
//!=========================================

// https://picsum.photos/1280/720?random=1

// const imageEl = document.querySelector('.hero__image');
// // console.log(imageEl);

// imageEl.setAttribute(
//   'src',
//   `https://picsum.photos/1280/720?random=${Math.round(Math.random() * 1000)}`,
// );

// imageEl.setAttribute('width', '500');
// imageEl.setAttribute('width', '1000');

// console.log(imageEl.getAttribute('width'));

//!=========================================

/*
 * Атрибути
 * - get(ім'я-атрибута)
 * - set(ім'я-атрибута)
 * - remove(ім'я-атрибута)
 * - has(ім'я-атрибута)
 */

/*
 * Data-атрибути
 */

// const actions = document.querySelectorAll('.actions button');
// const firstBtnElem = actions[0];

// console.log(firstBtnElem.dataset.action);
// console.log(firstBtnElem.dataset.title);

// firstBtnElem.dataset.hello = 'HELLO WORLD';
